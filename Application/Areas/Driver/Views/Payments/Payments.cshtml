@model List<Vehicle>
@{
    ViewData["Title"] = "Pagamentos";
}
        <table class="table-center" id="vehicles-id" style="width:70%" align="center">
            <thead>
                <tr>
                    <th style="width:15%"><font size="5"><b><font>Modelo</font></b></font></th>
                    <th style="width:7%"><font size="5"><b><font>Placa</font></b></font></th>
                    <th style="width:3%"><font size="5"><b><font>Ação</font></b></font></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var vehicles in Model)
                {
                    <tr>
                        <td><b><font>@vehicles.Model</font></b></td>
                        <td><b><font>@vehicles.LicensePlate</font></b></td>
                        <td>
                            <a class="btn btn-animate mb-2 btn-animate-side btn-primary" onclick="pagar(@vehicles)">
                                <i class="icon wb-shopping-cart" aria-hidden="true"></i>Pagar
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

@section scripts{
    <script>

        function pagar(Vehicle vehicle) {

            var body = {
                vehicle
            }

            jQuery.ajax({
                url: '@Url.Action("Paypal", "Paypal")',
                type: "POST",
                data: JSON.stringify(),
                dataType: "json",
                contentType: "application/json; charset=utf-8",
                success: function (data) {

                    console.log(data);
                    if (data.status) {

                        var jsonresult = JSON.parse(data.respuesta);

                        console.log(jsonresult);

                        var links = jsonresult.links;

                        var resultado = links.find(item => item.rel === "approve")

                        window.location.href = resultado.href

                        /*console.log(links)*/
                        /*console.log(resultado)*/
                    } else {
                        alert("Vuelva a intentarlo más tarde")
                    }


                },
                beforeSend: function () {
                }
            });
        }
    </script>

    }